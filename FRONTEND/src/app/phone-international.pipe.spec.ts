import { PhoneInternationalPipe } from './phone-international.pipe';

describe('PhoneInternationalPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneInternationalPipe();
    expect(pipe).toBeTruthy();
  });
});
